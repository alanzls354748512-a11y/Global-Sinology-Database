name: Global Sinology Academic Sync

on:
  schedule:
    - cron: '0 19 * * *' # 北京時間凌晨 03:00 自動執行
  workflow_dispatch:      # 允許手動觸發測試

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install importlib-metadata google-api-python-client google-auth-httplib2 google-auth-oauthlib
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run Scholar Processor
        env:
          # 確保您的 GitHub Secrets 中已設定 GDRIVE_CREDENTIALS
          GDRIVE_CREDENTIALS: ${{ secrets.GDRIVE_CREDENTIALS }}
          PYTHONPATH: .
        run: python Scholar_Processor.py
